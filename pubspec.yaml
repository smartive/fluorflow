name: fluorflow
description: A new Flutter package project.
version: 0.0.1
homepage: todo

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.5
  get_it: ^7.6.0

dev_dependencies:
  build_runner: ^2.4.6
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  mockito: ^5.4.2

flutter: null
