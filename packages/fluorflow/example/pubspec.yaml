name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  fluorflow: ^1.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.4.6
  fluorflow_generator: ^1.0.0
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

dependency_overrides:
  fluorflow:
    path: ../
  fluorflow_generator:
    path: ../../fluorflow_generator
